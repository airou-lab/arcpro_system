---
phase: 03-retraining
plan: 02
type: execute
wave: 1
depends_on: ["03-01"]
files_modified: ["src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/track_manager.py", "src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/hud.py", "src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/arcpro_env_cfg.py"]
autonomous: true

must_haves:
  truths:
    - "Laps are correctly counted when the robot crosses the start/finish line."
    - "A HUD overlay is visible in the Isaac Sim viewport showing lap count and speed."
  artifacts:
    - path: "src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/track_manager.py"
      provides: "Lap counting logic"
    - path: "src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/hud.py"
      provides: "omni.ui based HUD overlay"
  key_links:
    - from: "TrackManager.update_laps"
      to: "HUD.update"
      via: "Environment step loop"
---

<objective>
Enhance the simulation environment with automated lap counting and a real-time HUD for performance monitoring.

Purpose: Provide objective metrics (laps) and visual feedback during both training and verification.
Output: Vectorized lap counting in TrackManager and an omni.ui HUD.
</objective>

<execution_context>
@/home/arika/.gemini/get-shit-done/workflows/execute-plan.md
@/home/arika/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-retraining/03-01-SUMMARY.md
@src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/track_manager.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vectorized Lap Counting in TrackManager</name>
  <files>src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/track_manager.py</files>
  <action>
    Modify `TrackManager` to track lap progress for all agents:
    - Add `self.lap_count` and `self.last_wp_idx` as torch tensors (initialized to zeros).
    - Implement `update_laps(pos: torch.Tensor)`:
      1. Find current closest waypoint index for each agent.
      2. Detect "Forward" crossing: `last_wp_idx` is near the end (e.g., > 90% of total) and `current_wp_idx` is near the start (e.g., < 10%).
      3. Increment `lap_count` on forward crossing.
      4. Handle resets: If an agent is reset (detected by a large jump or external signal), reset its `last_wp_idx` without incrementing laps.
    - Ensure logic is fully vectorized for 128+ agents.
  </action>
  <verify>
    Run a test script that moves a dummy position around the track and confirms `lap_count` increments correctly in `TrackManager`.
  </verify>
  <done>
    `TrackManager` correctly tracks and returns lap counts for all agents in a vectorized manner.
  </done>
</task>

<task type="auto">
  <name>Task 2: Isaac Lab 2.0 HUD Overlay (omni.ui)</name>
  <files>src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/mdp/hud.py, src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/arcpro_env_cfg.py</files>
  <action>
    Create a real-time HUD using the `omni.ui` framework:
    - Create `mdp/hud.py` with an `ARCProHUD` class.
    - Use `omni.ui.Window` with `flags=omni.ui.WINDOW_FLAGS_NO_SCROLLBAR | omni.ui.WINDOW_FLAGS_NO_TITLE_BAR` for a transparent overlay.
    - Display the following for Environment 0:
      - Lap Count (from TrackManager).
      - Current Speed (linear velocity magnitude).
      - Lateral Error (from TrackManager).
    - Implement an `update(env)` method that refreshes the UI labels.
    - Integrate into `ARCProEnvCfg`: add a hook to initialize the HUD if `args.video` or a new `enable_hud` flag is set.
  </action>
  <verify>
    Launch the simulator with `--video` (or HUD enabled) and verify the overlay appears in the viewport and updates as the robot moves.
  </verify>
  <done>
    A functional HUD overlay is visible in Isaac Sim, providing real-time telemetry.
  </done>
</task>

</tasks>

<verification>
Check that `TrackManager` lap counts are accessible and that the HUD displays correct values during a manual teleop test or short training run.
</verification>

<success_criteria>
- Vectorized lap counting logic handles wrap-around correctly.
- HUD displays real-time data for at least one agent (Env 0).
- No performance regression in simulation FPS.
</success_criteria>

<output>
After completion, create `.planning/phases/03-retraining/03-02-SUMMARY.md`
</output>
