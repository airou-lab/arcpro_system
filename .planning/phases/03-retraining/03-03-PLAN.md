---
phase: 03-retraining
plan: 03
type: execute
wave: 2
depends_on: ["03-02"]
files_modified: ["scripts/verify_policy.py"]
autonomous: false

must_haves:
  truths:
    - "Trained policy can complete 10 consecutive laps."
    - "Policy maintains lateral error below 0.3m throughout the run."
  artifacts:
    - path: "scripts/verify_policy.py"
      provides: "Policy inference and metrics script"
  key_links:
    - from: "scripts/verify_policy.py"
      to: "best_checkpoint.pt"
      via: "Model loading"
---

<objective>
Execute the final verification run for Phase 3, proving that the trained policy meets the project's autonomous driving performance targets.

Purpose: Final validation of Phase 3 outcomes.
Output: Verification report for 10 laps.
</objective>

<execution_context>
@/home/arika/.gemini/get-shit-done/workflows/execute-plan.md
@/home/arika/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-retraining/03-02-SUMMARY.md
@src/examples/ARCPro_RL/arc_rl_isacc_sim/arcproLab/train.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: scripts/verify_policy.py inference script</name>
  <files>scripts/verify_policy.py</files>
  <action>
    Create a standalone verification script:
    - Port logic from `train.py` to handle loading a trained policy checkpoint from `logs/rsl_rl/arcpro_retraining/`.
    - Setup the environment with `num_envs=1` and `enable_hud=True`.
    - Run the policy in inference mode (no gradients, deterministic actions if possible).
    - Track metrics across multiple laps:
      - Lap times.
      - Max and Average lateral error.
      - Success/Failure (10 laps target).
    - Exit after 10 laps or failure.
  </action>
  <verify>
    Run the script with a random policy or early checkpoint and confirm it starts correctly, loads the model, and displays telemetry.
  </verify>
  <done>
    A dedicated verification script is ready for the final 10-lap run.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 10-lap Autonomous Verification Run</name>
  <what-built>A trained RL policy with lap-tracking and HUD support.</what-built>
  <how-to-verify>
    1. Identify the best checkpoint from the latest retraining run (e.g., `logs/rsl_rl/arcpro_retraining/.../model_best.pt`).
    2. Run the verification script: 
       `./isaaclab.sh -p scripts/verify_policy.py --checkpoint path/to/model_best.pt`
    3. Observe the robot completing 10 laps.
    4. Confirm in the HUD or console output that:
       - Laps = 10 reached.
       - Max Lateral Error remains below 0.3m.
  </how-to-verify>
  <resume-signal>Describe the results: confirm laps completed and max lateral error recorded.</resume-signal>
</task>

</tasks>

<verification>
Check final logs for lap completion and error metrics.
</verification>

<success_criteria>
- 10 laps completed without crash or off-track reset.
- Max lateral error < 0.3m.
- Verification script produces a clear summary of performance.
</success_criteria>

<output>
After completion, create `.planning/phases/03-retraining/03-03-SUMMARY.md`
</output>
